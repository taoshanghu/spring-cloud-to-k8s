apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    k8s-app: pfm
  name: pfm
spec:
  replicas: 1
  selector:
    matchLabels:
      name: pfm
      release: test
  template:
    metadata:
      labels:
        name: pfm
        release: test
    spec:
      containers:
      - name: pfm
        image: docker.ysd.com/socddp/socddp-pfm:V5
        ports:
        - name: config
          containerPort: 8130
        livenessProbe:
          httpGet:  
            port: 8130
            path: /info
          initialDelaySeconds: 240
          timeoutSeconds: 5
        resources:
          requests:
            memory: "512Mi"
            cpu: "100m"
          limits:
            memory: "1024Mi"
            cpu: "500m"
        env:
        #redis
        - name: REDIS_HOST
          value: "redis-master.default.svc.cluster.local"
        - name: REDIS_PORT
          value: 
        - name: REDIS_PASS
          value: "12345678"
        #mysql
        - name: DB_HOST
          value: "192.168.100.40"
        - name: DB_NAME
          value: "socddp"
        - name: DB_USER
          value: "socddp-user"
        - name: DB_PASS
          value: "ddf1Utb8J%GgyFY0"
        #mq
        - name: MQ_HOST
          value: "192.168.100.51"
        - name: MQ_USER
          value: "admin"
        - name: MQ_PASS
          value: "ysd342501"
        #eureka
        - name: SVC_NAME
          value: "socddp-eureka"
        - name: "REPL"
          value: "3"
        #mode_name
        - name: MODE_NAME
          value: "pfm"
---
apiVersion: v1
kind: Service 
metadata:
  labels:
    name: pfm
    release: test
  name: pfm
spec:
  ports:
  - name: pfm
    port: 8130
    targetPort: 8130
  selector:
    name: pfm
    release: test
